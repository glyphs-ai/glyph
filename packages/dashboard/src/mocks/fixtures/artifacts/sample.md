# Designer mode markdown artifact

This file exercises the dashboard's `MarkdownViewer` from designer mode.

## What it covers

- Headings (h1 / h2)
- Bullet lists
- `inline code` spans
- Links: [glyph](https://github.com/glyphs-ai/glyph)

```ts
// fenced code block — rendered as text by the lightweight markdown viewer
export const greeting = "hello from MSW";
```

Edit `src/mocks/fixtures/artifacts/sample.md` to customise the preview.

{
  "name": "@glyphs-ai/api",
  "private": true,
  "version": "0.0.0",
  "description": "Application layer for glyph: orchestration of T0/T1 packages into a composable runtime, plus the wire contracts under its `wire/` surface (re-exported from the barrel so server has a single import site). Dashboard / cli depend on `@glyphs-ai/sdk` instead.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@glyphs-ai/catalog": "workspace:*",
    "@glyphs-ai/runtime": "workspace:*",
    "@glyphs-ai/schedule": "workspace:*",
    "@glyphs-ai/session": "workspace:*",
    "@glyphs-ai/task": "workspace:*",
    "@glyphs-ai/terminal": "workspace:*",
    "@glyphs-ai/workflow": "workspace:*",
    "@glyphs-ai/workspace": "workspace:*",
    "@hono/zod-openapi": "^1.4.0",
    "hono": "^4.7.11",
    "pino": "^9.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/glyphs-ai/glyph.git",
    "directory": "packages/api"
  }
}

# Changelog

## 0.0.2 (2026-06-11)

- Rename `taskDir` constructor param to `workdir` on `RuntimeHeadlessLaunchFailed` for tier-boundary consistency with `RuntimeProvisionFailed`. Also renames the same field on `LaunchCopilotHeadlessOpts`. Internal rename — `instanceof` / `.name` / `code` / positional-arg consumers are unaffected.

## 0.0.1

- Initial release.

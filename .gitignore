node_modules/
dist/
dist-native/
bundle/
*.tsbuildinfo
.DS_Store
.pnpm-store/
coverage/
.vitest-cache/
*.log
packages/catalog/dist/
packages/dashboard/dist/
packages/server/dist/

# Local commit-message scratch files
.commit-msg*.txt

# Designer agent runtime output (dev-server logs + screenshots from
# Playwright probes). Per-workspace, never source-controlled.
.designer/

# Playwright MCP per-workspace storage state (login cookies, session
# state). Per-workspace, never source-controlled.
.playwright/

# Local-only dev scripts (db migrations, ad-hoc probes)  not part of the published toolchain.
scripts/local/

